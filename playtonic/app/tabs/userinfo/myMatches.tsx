import { Text, View } from "react-native";

const MyMatches = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>MyMatches works</Text>
    </View>
  );
};

export default MyMatches;
