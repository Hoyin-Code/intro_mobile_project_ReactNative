import { Text, View } from "react-native";
// TODO: show all matches where you are in and where the score is not entered yet
// if you are the host and the match was completed highlight them that you need to add the results if not done yet
const MyMatches = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>MyMatches works</Text>
    </View>
  );
};

export default MyMatches;
